# Cloudflare Workers deployment runbook

> Status, updated 2026-09-26: [GitHub deploy Action 36132611731](https://github.com/kleedaisuki/kleedaisuki.github.io/actions/runs/36132611731) deployed the Rust Worker plus SSG assets; [manual cutover Action 36133244601](https://github.com/kleedaisuki/kleedaisuki.github.io/actions/runs/36133244601) proxied the canonical CNAME; [deploy Action 36135578884](https://github.com/kleedaisuki/kleedaisuki.github.io/actions/runs/36135578884) released improved mail action pages and queued one HTML update. The owner confirmed that update arrived in **Outlook's spam folder**. A separate confirmation email to a **Cloudflare-unverified** school destination reached the owner's inbox and Cloudflare logged `delivered`. **General cross-domain inbox placement, Billing UI paid-plan status, and global DNS propagation are not verified.** See [the validation record](cloudflare-validation.md). Keep secrets out of Git.

Those Actions establish that the configured GitHub credentials could deploy this Worker, apply the remote D1 migration, and PATCH the intended DNS record at that time. They do not prove future token validity, broader permissions, mail sending eligibility, or delivery. The setup below is the repeatable release/rollback contract.

## Delivery shape and compatibility

The Astro build produces pre-rendered HTML in `dist/`. Root `wrangler.jsonc` publishes those static assets together with the Rust/Wasm Worker from `worker/`. Static content should be served as assets when it exists; only API paths need Worker execution. This preserves crawlable HTML, existing URLs, the site's theme/layout and the feed/sitemap/`llms.txt` endpoints without introducing a client-side-rendering dependency. Review changes to `wrangler.jsonc`, route ownership and asset handling together: a successful Wasm compilation alone does not prove that static routes remain reachable.

The canonical host is `atelier.moesegfault.dev`. A Cloudflare [Workers Route](https://developers.cloudflare.com/workers/configuration/routing/routes/) at `atelier.moesegfault.dev/*` is attached. The existing CNAME still targets `kleedaisuki.github.io`, but its `proxied` flag is now **true** after the successful manual cutover; a Route receives traffic only when that flag is true. The cutover did **not** delete or replace the CNAME. Setting the flag back to `false` bypasses Cloudflare and restores direct GitHub Pages traffic after DNS caches refresh. This deliberately differs from a Workers Custom Domain, which conflicts with an existing CNAME. See [Cloudflare proxy status](https://developers.cloudflare.com/dns/proxy-status/). The repository `CNAME` file remains necessary for GitHub Pages rollback and does not configure Workers.

## One-time Cloudflare and GitHub setup

1. `moesegfault.dev` is an active Cloudflare DNS zone. D1 `atelier-subscriptions` and migration `worker/migrations/0001_init.sql` are provisioned/applied remotely, and root `wrangler.jsonc` declares the `DB`, `EMAIL`, and `SUBSCRIBE_LIMIT` bindings. For a new account or recovery, provision these before deploy. **Sending to arbitrary opt-in subscribers requires Workers Paid**, not the Free plan; verify the actual account plan and quota before live subscriber mail ([Email Service pricing](https://developers.cloudflare.com/email-service/platform/pricing/)).
2. Create a narrowly scoped Cloudflare API token with Workers deployment, **D1 Edit**, and zone-scoped **DNS Read/Write** permissions, limited to the target account and zone. Save the token and account ID as GitHub Actions repository secrets `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`. The workflow intentionally fails rather than silently skips production deployment when either is missing. See [Cloudflare's GitHub Actions guide](https://developers.cloudflare.com/workers/ci-cd/external-cicd/github-actions/), [D1 migration commands](https://developers.cloudflare.com/d1/wrangler-commands/), and [DNS API permissions](https://developers.cloudflare.com/api/resources/dns/subresources/records/methods/edit/).
3. `ATELIER_NOTIFY_TOKEN` is already configured as both a Worker runtime secret and a GitHub Actions secret. They must hold the **same value**; rotate them together. For recovery, use `pnpm exec wrangler secret put ATELIER_NOTIFY_TOKEN` (or the dashboard), then update GitHub. This protects `POST /api/admin/notify` and `GET /api/admin/status`; it is not an email-provider API key. The native `send_email` binding supplies Cloudflare Email Service without a provider credential. Do **not** put the admin token under `vars`, commit `.dev.vars`, or print it. `wrangler secret put` creates/deploys a Worker version; coordinate rotation with release. See [Workers secrets](https://developers.cloudflare.com/workers/configuration/secrets/).
4. The Email Sending domain is enabled. One update to an owned Cloudflare-verified Outlook mailbox reached D1 `sent` and the spam folder; a separate confirmation to an owner-controlled, **Cloudflare-unverified** school address reached its inbox, corroborated by one Cloudflare `delivered` GraphQL event. This establishes functional acceptance for that one unverified address, **not** general recipient-domain delivery or inbox rates. Cloudflare defines `delivered` as recipient-server acceptance ([Email logs](https://developers.cloudflare.com/email-service/observability/logs/)). [Pricing](https://developers.cloudflare.com/email-service/platform/pricing/) says arbitrary recipients require Workers Paid; the actual Billing UI plan/quota remain unchecked. Before broad dispatch, check current SPF/DKIM/DMARC/bounce DNS, sender eligibility, plan/quota, and received one-click-unsubscribe headers. See [Email Sending setup](https://developers.cloudflare.com/email-service/get-started/send-emails/) and [send binding restrictions](https://developers.cloudflare.com/email-service/configuration/send-bindings/). The notification-versus-bulk policy nuance is in [the mail architecture](cloudflare-worker-mail-architecture.md#email-provider-fit-decision-and-remaining-evidence).
5. If a GitHub Environment is used for approval or environment-scoped secrets, adapt the workflow to use a separate deploy job gated on `main` and that environment; the current single-job workflow reads **repository** secrets only at the deploy step.

## CI and release

`.github/workflows/deploy.yml` runs on pull requests, pushes to `main`, and manual dispatch. It installs pinned pnpm/Node and the Rust Wasm target, checks Rust formatting and tests, runs Astro/type/unit/lint/format/build/SEO checks, validates D1 migrations locally, runs browser tests, then performs `wrangler deploy --dry-run` to exercise the combined bundle. Only a successful run on `main` applies D1 migrations remotely and deploys via Cloudflare's official [`wrangler-action@v4`](https://github.com/cloudflare/wrangler-action). Review schema changes for backward compatibility: migrations run **before** the new Worker, so the old Worker must tolerate the new schema during rollout or rollback. Diagnostic browser artifacts upload even when tests fail. The workflow grants only `contents: read`; it does not use GitHub Pages permissions or upload a Pages artifact.

On a **push to `main` only**, after Worker deployment succeeds, `scripts/dispatch-notifications.mjs` compares the push's before/after commits and submits changed `notifications/*.json` manifests **or manifests whose referenced sibling HTML or text changed** to `POST /api/admin/notify`. GitHub repository variable `ATELIER_WORKER_URL` is the live HTTPS Worker origin (the `workers.dev` origin works independently of DNS proxy state), and repository secret `ATELIER_NOTIFY_TOKEN` matches the Worker secret. No changed notification asset means no token or URL is needed. Manual dispatch never sends notifications. `send:false` saves/edits a draft; only explicit `send:true` queues. The Worker enforces idempotency by `id` and rejects changed content after a campaign is queued or sent.

### Author and release a new notification

1. Choose a **new stable campaign ID** and a genuinely published canonical article/work URL. In `notifications/`, add a manifest, sibling HTML fragment, and sibling substantive plain-text file. For the shared Atelier shell, use `{ "id": "new-id", "subject": "...", "format": "atelier-fragment-v1", "html_file": "issue.html", "text_file": "issue.txt", "send": false }`; [`notifications/atelier-update.json`](../notifications/atelier-update.json) is a current draft example. The fragment contains only issue-specific title, summary, and direct content CTA—no document root, script/form, or unsubscribe placeholder. Its text file contains corresponding title/summary and canonical HTTPS content URL, without boilerplate. Rust adds the common frame, reason, and unsubscribe **placeholder** before storing complete HTML/text snapshots; the recipient-specific link is substituted only at send time.
2. Review the rendered full HTML and text, subject, canonical destination, recipient timing, and relevant client behavior. A `send:false` main push updates only that draft; changes to its `.html` **or** `.txt` select the manifest too. Do not infer Gmail/Outlook compatibility from a browser preview; actual new-template client rendering remains unverified until tested.
3. Only after explicit approval, change the **same still-draft** manifest to `send:true` and push to `main`. CI deploys first, then the protected Worker freezes the complete HTML/text and snapshots currently confirmed recipients. Monitor `GET /api/admin/status?id=...` for `queued`, `sent`, `unknown`, and `complete` counts; reconcile `unknown` rather than blindly retrying. Do not edit an already queued ID: changed content returns 409. For another update use a fresh ID, not a modified launch campaign.

Legacy manifests with omitted/`document` format remain full HTML documents and must carry `{{unsubscribe_url}}`; optional legacy text requires its own placeholder. Existing queued campaigns and previously sent mail are not rewrapped. **Never change old `send:true` manifest/HTML/text merely to restyle the mail or replay a historical campaign**: CI intentionally notices content changes and the Worker rejects changed queued content. A shell-only code change does not select old notification manifests, and this is the safe way to release style changes without a historical resend.

For a local preflight (the successful GitHub Actions run remains the release gate):

```sh
pnpm install --frozen-lockfile
cargo install worker-build --version '^0.8' --locked
pnpm run check && pnpm test && pnpm run build
pnpm run verify:seo
cargo test --manifest-path worker/Cargo.toml --locked
node --test tests/notification-dispatch.test.mjs
pnpm exec wrangler d1 migrations apply DB --local --persist-to .cache/wrangler
pnpm exec wrangler deploy --dry-run
```

`wrangler deploy --dry-run` checks packaging but cannot verify account permissions, remote bindings, DNS, Email Service onboarding, or live delivery. The `main` push deployment path was exercised successfully by [Action 36132611731](https://github.com/kleedaisuki/kleedaisuki.github.io/actions/runs/36132611731): 84 browser tests passed, 7 were skipped, remote D1 migration ran, Worker version `de9e3e28-faa4-4b3e-8e0b-38cca6918b18` deployed, and the changed sample notification manifest was accepted with `send:false`, leaving a D1 draft rather than mailing subscribers. Manual workflow dispatch from another branch validates but does not deploy. For the **new** nullable `campaigns.text` column, migration `worker/migrations/0002_campaign_text.sql` must likewise apply **before** deploying the Worker that reads it; the workflow orders remote migrations before deployment. It is additive, so old `text=NULL` campaigns and old Worker queries remain valid. Do not claim the new migration has run remotely until that Action succeeds.

The later [Action 36135578884](https://github.com/kleedaisuki/kleedaisuki.github.io/actions/runs/36135578884) also passed 84 browser tests with 7 skips and deployed Worker version `43b2e91e-7029-4bd2-b29d-8f93a65aad3f`. It accepted editable HTML campaign `atelier-worker-launch-2026-09-25` with `send:true`. Remote D1 observed one queued recipient, then `complete`/`sent` with `attempts=1` and no error at `sent_at=1790340059285` (2026-09-25 12:40:59.285 UTC). The user-owned Outlook mailbox had previously moved `pending → confirmed` via the canonical subscription/confirmation flow. The owner subsequently confirmed receipt in Outlook's **spam folder**, with HTML layout and unsubscribe link displayed normally. This establishes CI queuing, scheduled dispatch, and delivery to this **Cloudflare-verified** recipient; it does not establish inbox placement, one-click header behavior, or eligibility for arbitrary unverified recipients. The redesigned action pages passed independent live visual/header smoke; details are in [the validation record](cloudflare-validation.md).

On 2026-09-26, an owner-controlled school address absent from Cloudflare's verified Email Routing destinations submitted the canonical subscribe form. The Worker returned 202 and D1 recorded `pending`; the owner reported receiving the confirmation message in the school's **inbox** without clicking its link. One matching `emailSendingAdaptive` event at 2026-09-25T18:35:35Z recorded `delivered` with no error. A read-only limits check showed `usage.sent=1` of `quota=1000/day`, versus zero usage after the earlier verified-destination test, consistent with Cloudflare's [quota treatment](https://developers.cloudflare.com/email-service/platform/pricing/). The test's pending row was then conditionally removed (`changes=1`), and a follow-up check found no school row while the confirmed Outlook subscriber remained. No second message or update campaign was sent to that school address. Do not retain test subscribers for later campaigns without confirmation; do not publish recipient addresses, tokens, or message IDs. The event proves recipient-server acceptance under Cloudflare's [log semantics](https://developers.cloudflare.com/email-service/observability/logs/); the owner's report adds inbox placement for this single address, not broad deliverability or account billing-plan proof.

## Cutover smoke tests and rollback

For future activation or re-activation, inspect the deploy Action log and verify the `workers.dev` preview first. Run **Actions → Manual Cloudflare DNS cutover → activate** from `main` only when preview is healthy. The workflow checks pre-rendered `/zh/` and Rust `/api/health`, reads the exact CNAME, requires exactly one record targeting `kleedaisuki.github.io`, PATCHes only `proxied: true`, then waits for canonical page and Worker health to converge. It fails closed if permissions or records are ambiguous. `CLOUDFLARE_API_TOKEN` needs **DNS Read and DNS Write** for this zone. `ATELIER_WORKER_URL` must be `https://atelier-moesegfault.moesegfault.workers.dev/` for preview verification. [Action 36133244601](https://github.com/kleedaisuki/kleedaisuki.github.io/actions/runs/36133244601) exercised this path: the DNS flag was patched at 12:08:45 UTC and canonical checks converged at 12:13:46 UTC. This is a sampled successful cutover, **not** proof of worldwide propagation. See [DNS list](https://developers.cloudflare.com/api/resources/dns/subresources/records/methods/list/) and [DNS PATCH](https://developers.cloudflare.com/api/resources/dns/subresources/records/methods/edit/).

After activation, check both localized homepages and representative blog/articraft pages with `curl` or a browser, then verify `/rss.xml`, `/llms.txt`, sitemap URLs, original downloadable assets, and a nonexistent URL's 404 behavior. Confirm HTML contains expected title/canonical/hreflang and body content without executing JavaScript. Test the subscription flow with an owned mailbox, including consent/unsubscribe semantics and an actual rendered HTML message; never mail an unconsenting address to smoke-test production.

If cutover is defective, run **Actions → Manual Cloudflare DNS cutover → rollback** from `main`. It PATCHes only `proxied: false` on that same validated CNAME, verifies the canonical page still works, and requires the Worker health endpoint to disappear. DNS cache propagation can take minutes; a failed post-switch health check does not silently issue a second DNS change, so inspect the current DNS flag and rerun rollback if necessary. For a defect in a later Worker release after cutover, Cloudflare's [deployment rollback](https://developers.cloudflare.com/workers/configuration/versions-and-deployments/rollbacks/) or redeployment of a known-good Git revision may be more appropriate. Static asset and Worker versions should be rolled back together, not independently.

## Sources and decision notes

- [Cloudflare Workers: Rust](https://developers.cloudflare.com/workers/languages/rust/) documents the `worker-build`-generated JavaScript entry point and Wasm bundle used by Wrangler.
- [Cloudflare Workers: Static Assets](https://developers.cloudflare.com/workers/static-assets/) documents uploading the build directory and serving matching assets without invoking Worker code by default.
- [Cloudflare Workers: GitHub Actions](https://developers.cloudflare.com/workers/ci-cd/external-cicd/github-actions/) and [wrangler-action](https://github.com/cloudflare/wrangler-action) document CI authentication and the official deployment action.
